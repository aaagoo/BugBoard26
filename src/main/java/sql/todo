OK? filtrare e ordinare i risultati in base a criteri come tipologia, stato, priorità o altri parametri
OK quando un bug viene risolto, il sistema notifica l'utente che lo ha segnalato
OK? è necessaria una funzione di ricerca che consenta di trovare bug in base a parole chiave
OK il sistema può gestire automaticamente l'assegnazione di bug, o può farlo manualmente l'utente
OK spostare icona dell'utente e bottone dell'area personale in alto a destra

